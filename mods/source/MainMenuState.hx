import flixel.FlxSprite;

function onCreate():Void {
    trace('CREATED STATE');

    /*
    final TESTING:FlxSprite = new FlxSprite().makeGraphic(1*2,1*2, FlxColor.RED);
    TESTING.screenCenter();
    add(TESTING);
    */
}

function onUpdate(elapsed:Float):Void {
    /*trace('UPDATE ELAPSED: ' + elapsed);*/
}